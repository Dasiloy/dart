void main() {
  // print({double.parse('32'), int.parse("23")});

  // String words = 'Hello, NAME';
  // var greetBob = words.replaceAll(RegExp('NAME'), "Bob");
  // print(greetBob);

  // var indicies = List.generate(4, (ind) {
  //   print(ind);
  //   return ind.toString();
  // });
  // print(indicies);

  // var sets = Set.from(["a", "b", "c"]);
  // print(sets);

  // var teas = ['green', 'black', 'chamomile', 'earl grey', 'black'];
  // var uniqueTeas = teas.toSet();
  // print(uniqueTeas);
}
